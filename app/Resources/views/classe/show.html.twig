{% extends 'layout.html.twig' %}

{% block body %}
<div id="panel-content">
    <div class="tab-content">
        <div class="tab-pane fade active in" id="accueilpanel" role="tabpanel" aria-labelledby="nav-contact-tab">
            <h1>Détails d'une classe</h1>

            <table>
                <tbody>
                    <tr>
                        <th>Libelle</th>
                        <td>{{ classe.libelle }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('classe_index') }}">Retour a la liste</a>
                </li>
                <li>
                    <a href="{{ path('classe_edit', { 'id': classe.id }) }}">Edition</a>
                </li>
                <li>
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Supression">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
{% endblock %}
